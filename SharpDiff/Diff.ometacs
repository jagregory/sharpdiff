using OMetaSharp;

ometa SharpDiff.DiffParser : Parser {
  Header = "diff" Space FormatType:format	-> { new Header(format.As<FormatType>()) },
  
  FormatType = "--" Text:format				-> { new FormatType(format.As<string>()) },
  Text = LetterOrDigit+
}